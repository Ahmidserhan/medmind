# Supabase Configuration
NEXT_PUBLIC_SUPABASE_URL=your-supabase-url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key

# Google OAuth (if using Google authentication)
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret

# WHO ICD-11 API (Optional - Free registration at https://icd.who.int/icdapi)
# Get official ICD-11 codes from World Health Organization
# Without these, the app falls back to ICD-10-CM (also free, no registration)
WHO_ICD_CLIENT_ID=your-who-client-id
WHO_ICD_CLIENT_SECRET=your-who-client-secret

# Note: Medical Glossary works without WHO credentials using free fallback APIs
